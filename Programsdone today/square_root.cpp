#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    float a,result;
    cout<<"Enter the number a: ";
    cin>>a;
    result=sqrt(a);
    cout<<"Square root of the number is: "<<result;
    return 0;
}